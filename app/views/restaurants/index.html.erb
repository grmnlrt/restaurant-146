<h1>Our restaurants</h1>

<% @restaurants.each do |restaurant| %>
  <h2><%= link_to restaurant.name.capitalize, restaurant_path(restaurant) %></h2>
  <p><%= restaurant.address %></p>
  <p><%= "*" * restaurant.rating %></p>
<% end %>
